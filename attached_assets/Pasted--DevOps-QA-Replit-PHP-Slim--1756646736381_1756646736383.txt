Представь, что ты безошибочный DevOps-QA-бот в Replit, который разворачивает PHP/Slim-приложение, автоматически создаёт БД, находит и исправляет все ошибки, прогоняет unit- и E2E-тесты, визуальный регресс и не остановится, пока проект не станет точной копией https://pc.dfbiu.com/.

Контекст:
  - У тебя есть полный проект в Replit:  
    • public/, src/, admin/, migrations/, tests/, cypress/, scripts/, .github/.  
    • .env.example с переменными, но при запуске получаем “Connection refused” на MySQL.  
    • Нужны автоматические фиксы ошибок конфигурации, подключение БД, миграции и тесты.

Задача:
  1. Проверить настройки подключения к MySQL из .env. Если соединение отказано, создать БД и пользователя автоматически (через PDO или mysql CLI), обновить конфиг .env и перезапустить соединение.
  2. Запустить миграции (001–004) и seed super-admin. Гарантировать, что таблицы users, admin_logs, login_attempts созданы и доступны.
  3. Стартовать встроенный PHP-сервер (`php -S 0.0.0.0:5000 -t public`), проверить `/health.php` (должен вернуть 200 и { status: “ok” }).
  4. Прогнать PHPUnit (tests/) — все тесты должны быть зелёными.  
  5. Прогнать Cypress E2E (cypress/) и визуальный регресс со скриншотами, сравнить с baseline через pixelmatch, подтвердить совпадение ≥98%.
  6. Автоматически фиксить:
     - синтаксические ошибки PHP (unexpected token)  
     - неправильные пути в .htaccess и fetch-proxy.js  
     - CSRF/Rate-limit ошибки (419/429)  
     - CORS-ошибки  
     - любые 404/500 при API- и админ-маршрутах  
  7. Повторять цикл “тест → анализ логов → авто-патч → тест” до 100% успеха по всем фронту и бэкенду.

Формат результата:
  1. Лог-отчёт по каждому шагу (подключение БД → миграции → health → phpunit → cypress → pixelmatch).  
  2. Таблица найденных ошибок и применённых авто-патчей (с diff).  
  3. Финальный статус:  
     - MySQL-соединение: OK  
     - Миграции: OK  
     - Health endpoint: 200 OK  
     - PHPUnit: все tests Passed  
     - Cypress E2E: все сценарии Passed  
     - Visual diff: ≥98% match  

Ограничения:
  - Не выходить за рамки Replit: встроенный PHP-сервер, MySQL-сервис.  
  - Не изменять UI/UX статики без явной необходимости.  
  - Все исправления должны быть автоматическими (patch-файлы, скрипты).

Начни с:
```bash
bash scripts/full-qa.sh
и докладывай пошагово, пока проект не будет «зелёным» и визуально идеальным.